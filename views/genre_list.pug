extends layout

block content 
  
  if (referrer === 'delete')
    h3.deleted-msg Genre deleted!
  
  h1= title
  
  ul
    for genre in genre_list
      li.simple-list
        a(href='/genre/' + genre._id)= genre.label

extends layout

block content
  h1= title
  br
  p.welcome Welcome to my #{title}!
  
  if error
    p Error getting the content, sorry!
  else
    p.welcome Here's what we have so far:
    br
    ul
      li.index !{data.game_count} Games
      li.index !{data.producer_count} Producers
      li.index !{data.platform_count} Platforms 
      li.index !{data.genre_count} Genres
      li.index !{data.review_count} Reviews
      li.index !{data.wishlist_count} Wishlisted  
